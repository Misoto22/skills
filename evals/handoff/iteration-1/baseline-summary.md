# Handoff native synchronization baseline

The runtime contract changed: Codex imports require native registration and readback, Claude indexes cover every existing local account and preserve desktop identity, and continued copies return as preserved branches. The tuning assertions were corrected before any instruction edit. Holdout cases were unchanged.

## Failures

The published storage paragraphs incorrectly treated raw Codex rollouts as sufficient and described only the active Claude account. The continuation paragraph denied reverse propagation. Three narrowly scoped paragraph edits address those failures. Hook diagnostics remain a separate iteration.

## Existing behavior at risk

Keep readable context distinct from a byte-exact provider replay. Preserve encrypted/signed reasoning limits, originals and independent branches, installation disclosure, unrelated hook entries, and Codex hook trust. The independent holdout measures the unchanged installation boundary.

## Measurement

Use three samples per case, the with-skill arm, identical medium reasoning settings, the recorded DeepSeek deployment provenance, and the shared budget ledger. The raw baseline remains immutable. One preliminary judgment accepted a response that denied reverse synchronization; human review rejected that judgment without rewriting the machine record. The corrected baseline is the only baseline used by this iteration.

Corrected tuning result: 2/9 samples passed, no void samples. Holdout: 3/3 samples passed.
