# Watcher completion evidence

The first candidate at 939579f passed all six continuation and registration samples. All three diagnostic samples failed to identify the watcher completion report. This iteration uses those immutable tuning records as its baseline; the installation holdout passed 3/3. The earlier fingerprint bookkeeping defect remains separately tracked and must be resolved before any release claim.

The old hook section still describes translating each transcript from a hook and maintaining byte watermarks. The old discovery paragraph guesses the newest rollout in a directory. The reporting paragraph incorrectly equates a missing hook with a one-way watcher. Three bounded edits correct these runtime descriptions.

Preserve silent, non-failing hooks; report partial and failed synchronization honestly; retain native task identity and source bytes; preserve installation disclosure, unrelated hooks, and hook trust. The suite and held-out cases are unchanged.
