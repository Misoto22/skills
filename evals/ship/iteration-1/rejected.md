# Rejected edits — ship, iteration 1

**None.** All three edits were kept; see [benchmark-summary.md](benchmark-summary.md).

## Why this iteration is only three edits

`ship` carries eight duplications worth removing and the budget is three, so the
work is split across three iterations rather than widened into one:

1. this one — the early exit, stated four times, now stated once in 0c;
2. iteration 2 — branch naming, the home worktree, and `Flags` re-rendering the
   classification table;
3. iteration 3 — `Reporting` restating 0h, and the marker tables moving to
   `references/detection.md`.

`ITERATION.md`: *if three edits are not enough, the iteration is too wide. Split
it.* Past three simultaneous edits a moved gate cannot be attributed, and the
cheap response — revert all three — discards the one that worked.

## Do not re-propose as

**"Keep `Common paths` as a quick index of the four shapes a run takes."** It
reads like orientation and is a re-rendering of the step-classification table,
which is the thing preflight actually computes. Two statements of when a step
runs is one that can be corrected and one that goes on being read.
