# Rejected edits — iteration 1

**None recorded.**

[forward-summary.md](forward-summary.md) reports three tightenings, all kept:
the ending contract, the personal-evidence audit, and the current-release
contract. No edit is recorded as tried and dropped.

Read that as a gap in the record rather than as three first attempts that all
landed. Each tightening was measured by re-running the prompt that had exposed
the failure — "after the ending contract was tightened, a focused rerun stayed
inside the inquiry" — so an intermediate wording that missed would have left no
trace in the summary or in the skill.

## What iteration-1 cannot claim

The suite carried no holdout split when this ran, and the three tightenings were
each scored on the prompt that produced them. `review-cultural-work` and
`voice-samples-without-biography` are now this suite's held-out cases; neither
was among the four prompts iteration-1 ran, so both are clean. Iteration-2 is
the first that can produce a gated number.
