interface:
  display_name: "Synastry Reading"
  short_description: "Validate and interpret synastry JSON v2 evidence"
  default_prompt: "Use $synastry-reading to validate an existing synastry JSON v2 artifact and write its adaptive, evidence-linked Markdown report."
policy:
  allow_implicit_invocation: true
