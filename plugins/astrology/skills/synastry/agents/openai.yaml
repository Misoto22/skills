interface:
  display_name: "Synastry"
  short_description: "Calculate uncertainty-aware synastry JSON v2"
  default_prompt: "Use $synastry to calculate an uncertainty-aware synastry JSON v2 artifact from two supplied birth records and hand a successful result to $synastry-reading."
policy:
  allow_implicit_invocation: true
