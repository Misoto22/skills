interface:
  display_name: "Cleanup"
  short_description: "Delete merged branches, worktrees, and stranded residue"
  default_prompt: "Use $cleanup to remove merged branches, their worktrees, and ignored residue; list everything first, verify each deletion against the forge, and keep whatever you cannot explain."
policy:
  allow_implicit_invocation: true
